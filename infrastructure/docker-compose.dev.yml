# Dev overrides: run backend locally with hot reload; only infra in Docker.
services:
  backend:
    profiles:
      - full

  redis:
    ports:
      - "6379:6379"

  ollama:
    ports:
      - "11434:11434"

  n8n:
    ports:
      - "5678:5678"
