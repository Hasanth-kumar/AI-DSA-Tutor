# Dev default (set in .env.development): http://127.0.0.1:3000
# Production behind nginx on same host: leave empty for same-origin /api
# Production without nginx: http://your-api-host:3000
VITE_API_BASE_URL=
